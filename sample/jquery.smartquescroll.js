// Generated by CoffeeScript 1.7.1
(function() {
  var MobilePage, Page;

  MobilePage = (function() {
    function MobilePage(el, options) {
      this.el = el;
      this.options = options;
    }

    return MobilePage;

  })();

  Page = (function() {
    function Page(el, options) {
      this.el = el;
      this.options = options;
      this.wrapper = this.el.parent();
      this.wrapper.css('overflow', 'scroll');
      this.wrapper.on('scroll', (function(_this) {
        return function(e) {
          var _base;
          return typeof (_base = _this.options).scroll === "function" ? _base.scroll(e) : void 0;
        };
      })(this));
    }

    return Page;

  })();

  (function($) {
    return $.fn.smartquescroll = function(options) {
      options = $.extend({
        scroll: null
      }, options);
      this.each(function() {
        if (navigator.userAgent.match(/iPhone|iPad|iPod|Android/)) {
          return new MobilePage($(this), options);
        } else {
          return new Page($(this), options);
        }
      });
      return this;
    };
  })(jQuery);

}).call(this);
